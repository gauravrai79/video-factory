# AI Influencer Factory — dependencies

# --- Core (CLI + pipeline) ---
requests>=2.31
python-dotenv>=1.0

# --- Optional prompt refinement (VF_USE_LLM=1) ---
# google-adk>=1.30           # Gemini-based still-prompt refinement

# --- Orchestration ---
# Default `sync` backend needs nothing extra (stdlib only) — runs on Windows.
# The `rq` backend (VF_QUEUE_BACKEND=rq) needs these + a reachable Redis (Linux/macOS workers):
# redis>=5.0
# rq>=1.16

# --- Console (API + web UI) ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- Postgres + delivery (prod; not required to run locally) ---
# sqlalchemy>=2.0
# psycopg[binary]>=3.1       # Postgres driver (Railway)
# google-api-python-client   # Google Drive delivery (later phase)

# Note: requires ffmpeg/ffprobe on PATH (or set FFMPEG_BIN / FFPROBE_BIN).
